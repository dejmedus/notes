{
  "open": [
    {
      "tab": "zsh",
      "commands": ["*close"],
      "shell": "bash"
    },
    {
      "tab": "git",
      "commands": ["git branch"],
      "icon": "git",
      "shell": "bash"
    },
    {
      "tab": "frontend",
      "commands": ["npm run dev"],
      "icon": "browser",
      "shell": "bash"
    }
  ],
  "close": [
    {
      "tab": "git",
      "commands": ["*close"],
      "description": "Close the terminal"
    },
    {
      "tab": "frontend",
      "commands": ["*stop", "*close"],
      "description": "Close the terminal"
    }
  ]
}
