# Visual Studio Code

`code .` Open file in VSCode

### Emmet

Emmet is a system of abbreviations that make working with HTML faster and easier. This is built-in to VS Code Editor.

#### Syntax

- parent>child
- sibling+sibling
- element.class

| Shortcut                        | HTML                                    |
| ------------------------------- | --------------------------------------- |
| `!html `                        | Generates boilerplate                   |
| `nav.navbar `                   | `<nav class="navbar"></nav>`            |
| `.container  `                  | `<div class="container"></div>`         |
| `button[type="submit"]{Submit}` | `<button type="submit">Submit</button>` |
| `nav>a.nav-item$\*>a   `        |                                         |
| `ul>li\*3.class-${List item $}` |                                         |

### Keyboard

- `command + enter` move to next line
- `option + delete` delete single word
- `tab` move into element

### Editor

- `command + p` search for files in directory
- `shift + command + p` search actions
- `command + f` search for word in file
- `control + \` open terminal

### Resources

- [VS Code Emmet User Guide](https://code.visualstudio.com/docs/editor/emmet)
- [Emmet Cheatsheet](https://docs.emmet.io/cheat-sheet/)
